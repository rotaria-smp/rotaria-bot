name: Go Build

on:
  push:
    paths:
      - 'cmd/bot/**'
      - 'internal/**'
      - 'go.mod'
      - 'Makefile'
      - '.github/workflows/go-build.yml'
  pull_request:
    paths:
      - 'cmd/bot/**'
      - 'internal/**'
      - 'go.mod'
      - 'Makefile'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - name: Build (Makefile)
        run: make build
      - name: Test (Makefile)
        run: make test
      # - name: Smoke run (logging init)
      #   env:
      #     ENV: dev
      #     LOG_LEVEL: debug
      #     LOG_PATH: ./logs/rotaria.log
      #     DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
      #   run: |
      #     ./bin/rotaria-bot || true
      #     echo "Generated log file (if any):"
      #     ls -l logs || true
      # - name: Upload logs
      #   if: always()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: build-logs
      #     path: logs/